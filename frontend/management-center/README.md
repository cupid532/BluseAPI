# BluseAPI Management Center (planned)

This directory is reserved for the first-party BluseAPI frontend application.

Intended target:
- root path `/` served by the backend
- same repository and release pipeline as backend
- replaces externally-fetched `management.html` over time
