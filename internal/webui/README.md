# internal/webui

This package is reserved for the future bundled BluseAPI web UI serving layer.

Planned responsibilities:
- serve first-party frontend assets from local build output or embedded assets
- provide app-shell fallback for `/`
- provide compatibility handling for `/management.html`
- isolate web UI delivery from legacy external management asset download logic
